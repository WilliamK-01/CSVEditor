# CSVEditor (Streamlit)

CSVEditor is a Streamlit-based transaction capture and review tool.

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run csveditor.py
```

## Scope

This repository contains the Streamlit app and Python core logic only.
